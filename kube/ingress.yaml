apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: court-case-service
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
    - hosts:
      - court-case-service.apps.live-1.cloud-platform.service.justice.gov.uk
  rules:
    - host: court-case-service.apps.live-1.cloud-platform.service.justice.gov.uk
      http:
        paths:
          - path: /
            backend:
              serviceName: court-case-service
              servicePort: 8000
